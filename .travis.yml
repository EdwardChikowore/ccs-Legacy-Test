language: ruby
cache:
  bundler: true
os: linux
dist: xenial
addons:
  chrome: stable
env:
  global:
    - CUCUMBER_ENV=test
before_install:
  - gem install bundler
script:
  - bundle exec cucumber -p travis
  - bundle exec cucumber -p clean_up
